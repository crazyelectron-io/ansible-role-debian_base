---
- name: UFW | Install package
  package:
    name: ufw
    state: present

- name: Allow SSH access
  community.general.ufw:
    rule: allow
    port: ssh
    proto: tcp

- name: Enable UFW
  community.general.ufw:
    state: enabled
